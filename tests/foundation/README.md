# Phase 0 RED evidence

These tests were committed before the Phase 0 API/package implementation.

- `FND-005`: fails until both Fastify API shells implement the normative health contract.
- `FND-006`: fails until all canonical packages exist and the pure domain boundary is materialized.

A failure caused by the explicit `FND-00x RED:` assertion is expected evidence of missing behavior, not a broken test harness. Subsequent Phase 0 commits must make these same tests GREEN without weakening the assertions.
